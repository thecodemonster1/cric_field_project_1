name: cric_field_project_1
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # tflite_flutter: ^0.11.0
  tflite_flutter: ^0.9.5
  tflite_flutter_helper: ^0.2.0
  percent_indicator: ^4.2.5
  fl_chart: ^0.70.2
  shared_preferences:
  # firebase_core: ^3.13.0

  # # Firebase packages
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  # cloud_firestore: ^5.6.6
  # firebase_analytics: ^11.4.5
  # intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    # Update this section to include the new model files
    - assets/model/enhanced_shot_placement_model.tflite
    - assets/model/enhanced_shot_type_model.tflite
